@model StudentFreelance.ViewModels.ReportListViewModel

@{
    ViewData["Title"] = "Danh sách báo cáo";
    Layout = "_Dashboard";
}

@section Styles {
    <link rel="stylesheet" href="~/css/report/listReport.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
}

<div class="container-fluid">
    <form asp-action="ListReport" method="get" class="topbar">
        <h1><i class="fas fa-flag"></i> Danh sách báo cáo</h1>

        <div class="filter-row">
            <div class="filter-group">
                <input type="text" name="SearchQuery" placeholder="🔍 Tìm kiếm..." value="@Model.SearchQuery" />
            </div>

            <div class="filter-group">
                <select name="SelectedStatusId" asp-items="Model.StatusFilter" class="form-select"></select>
            </div>

            <div class="filter-group">
                <select name="SelectedTypeId" asp-items="Model.TypeFilter" class="form-select"></select>
            </div>

            <button type="submit" class="filter-btn">
                <i class="fas fa-filter"></i> Lọc
            </button>
        </div>
    </form>
    
    <div class="table-container">
        @if (Model.Reports.Any())
        {
            <table class="styled-table" id="reportsTable">
                <thead>
                    <tr>
                        <th>Người báo cáo</th>
                        <th>Người bị báo cáo</th>
                        <th>Loại báo cáo</th>
                        <th>Dự án</th>
                        <th>Ngày báo cáo</th>
                        <th>Trạng thái</th>
                        <th>Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var report in Model.Reports)
                    {
                        <tr>
                            <td>@report.ReporterName</td>
                            <td>@report.ReportedUserName</td>
                            <td>@report.TypeName</td>
                            <td>
                                @if (!string.IsNullOrEmpty(report.ProjectTitle) && report.ProjectTitle != "Không có dự án" && report.ProjectID.HasValue)
                                {
                                    <a href="@Url.Action("Details", "Project", new { id = report.ProjectID })" title="Xem chi tiết dự án">
                                        @report.ProjectTitle
                                    </a>
                                }
                                else
                                {
                                    <span class="text-muted">Không có dự án</span>
                                }
                            </td>
                            <td>@report.ReportDate.ToString("dd/MM/yyyy HH:mm")</td>
                            <td>
                                @if (report.StatusName == "Đang xử lý")
                                {
                                    <span class="status-pending">@report.StatusName</span>
                                }
                                else if (report.StatusName == "Đã xử lý")
                                {
                                    <span class="status-active">@report.StatusName</span>
                                }
                                else if (report.StatusName == "Đã hủy")
                                {
                                    <span class="status-inactive">@report.StatusName</span>
                                }
                                else
                                {
                                    <span class="badge @(report.StatusName == "Đang xử lý" ? "bg-warning" : 
                                                  report.StatusName == "Đã xử lý" ? "bg-success" : 
                                                  report.StatusName == "Đã hủy" ? "bg-danger" : "bg-secondary")">
                                        @report.StatusName
                                    </span>
                                }
                            </td>
                            <td class="action-cell">
                                <a asp-action="Details" asp-route-id="@report.ReportID" class="action-btn edit">
                                    <i class="fas fa-eye"></i> Chi tiết
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            @if (Model.TotalPages > 1)
            {
                <div class="pagination-container">
                    <div class="pagination-info">
                        Hiển thị @(((Model.CurrentPage - 1) * Model.PageSize) + 1) đến @(Math.Min(Model.CurrentPage * Model.PageSize, Model.TotalCount)) của @Model.TotalCount báo cáo
                    </div>
                    
                    <div class="pagination">
                        @if (Model.CurrentPage > 1)
                        {
                            <a asp-action="ListReport" 
                               asp-route-currentPage="1"
                               asp-route-selectedStatusId="@Model.SelectedStatusId"
                               asp-route-selectedTypeId="@Model.SelectedTypeId"
                               asp-route-searchQuery="@Model.SearchQuery"
                               class="pagination-btn">
                                <i class="fas fa-angle-double-left"></i>
                            </a>
                            
                            <a asp-action="ListReport" 
                               asp-route-currentPage="@(Model.CurrentPage - 1)"
                               asp-route-selectedStatusId="@Model.SelectedStatusId"
                               asp-route-selectedTypeId="@Model.SelectedTypeId"
                               asp-route-searchQuery="@Model.SearchQuery"
                               class="pagination-btn">
                                <i class="fas fa-angle-left"></i>
                            </a>
                        }
                        
                        @{
                            int startPage = Math.Max(1, Model.CurrentPage - 2);
                            int endPage = Math.Min(Model.TotalPages, Model.CurrentPage + 2);
                            
                            if (startPage > 1)
                            {
                                <span class="pagination-ellipsis">...</span>
                            }
                        }
                        
                        @for (int i = startPage; i <= endPage; i++)
                        {
                            <a asp-action="ListReport" 
                               asp-route-currentPage="@i"
                               asp-route-selectedStatusId="@Model.SelectedStatusId"
                               asp-route-selectedTypeId="@Model.SelectedTypeId"
                               asp-route-searchQuery="@Model.SearchQuery"
                               class="pagination-btn @(i == Model.CurrentPage ? "active" : "")">
                                @i
                            </a>
                        }
                        
                        @{
                            if (endPage < Model.TotalPages)
                            {
                                <span class="pagination-ellipsis">...</span>
                            }
                        }
                        
                        @if (Model.CurrentPage < Model.TotalPages)
                        {
                            <a asp-action="ListReport" 
                               asp-route-currentPage="@(Model.CurrentPage + 1)"
                               asp-route-selectedStatusId="@Model.SelectedStatusId"
                               asp-route-selectedTypeId="@Model.SelectedTypeId"
                               asp-route-searchQuery="@Model.SearchQuery"
                               class="pagination-btn">
                                <i class="fas fa-angle-right"></i>
                            </a>
                            
                            <a asp-action="ListReport" 
                               asp-route-currentPage="@Model.TotalPages"
                               asp-route-selectedStatusId="@Model.SelectedStatusId"
                               asp-route-selectedTypeId="@Model.SelectedTypeId"
                               asp-route-searchQuery="@Model.SearchQuery"
                               class="pagination-btn">
                                <i class="fas fa-angle-double-right"></i>
                            </a>
                        }
                    </div>
                </div>
            }
        }
        else
        {
            <div class="alert alert-info">
                <i class="fas fa-info-circle me-2"></i> Không có báo cáo nào phù hợp với tiêu chí tìm kiếm.
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Auto-submit form when dropdown changes
            $('#SelectedStatusId, #SelectedTypeId').change(function () {
                $(this).closest('form').submit();
            });
        });
    </script>
} 