@model ErrorViewModel
@{
    ViewData["Title"] = "Lỗi";
}

<h1 class="text-danger">Lỗi.</h1>
<h2 class="text-danger">Đã xảy ra lỗi trong quá trình xử lý yêu cầu của bạn.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Mã yêu cầu:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Chế độ Phát triển (Development Mode)</h3>
<p>
    Khi chuyển sang môi trường <strong>Phát triển</strong>, hệ thống sẽ hiển thị thêm các thông tin chi tiết về lỗi đã xảy ra.
</p>
<p>
    <strong>Không nên kích hoạt môi trường Phát triển cho các ứng dụng đã triển khai thực tế.</strong>
    Điều này có thể làm lộ thông tin nhạy cảm về lỗi cho người dùng cuối.
    Để phục vụ cho việc gỡ lỗi cục bộ, bạn có thể kích hoạt môi trường <strong>Phát triển</strong> bằng cách đặt biến môi trường <strong>ASPNETCORE_ENVIRONMENT</strong> thành <strong>Development</strong>
    và khởi động lại ứng dụng.
</p>
