@model StudentFreelance.Models.Skill
@{
    ViewData["Title"] = "Ẩn kỹ năng";
}

<link rel="stylesheet" href="~/css/skills/hide.css" />

<div class="disable-skill-container">
    <div class="header-section">
        <h2>@ViewData["Title"]</h2>
        <span class="warning-icon">⚠️</span>
    </div>

    <div class="confirmation-card">
        <h4>Bạn có chắc chắn muốn ẩn kỹ năng này?</h4>
        <p class="info-text">
            Kỹ năng sẽ không bị xóa khỏi hệ thống nhưng sẽ không còn được dùng để tuyển dụng,
            tạo dự án hoặc gán cho sinh viên nữa.
        </p>

        <div class="skill-details">
            <dl class="row">
                <dt class="col-sm-3">Tên kỹ năng</dt>
                <dd class="col-sm-9">@Model.SkillName</dd>
                <dt class="col-sm-3">Nhóm nghề</dt>
                <dd class="col-sm-9">@Model.Category?.CategoryName</dd>
            </dl>
        </div>

        <form asp-action="Delete" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="SkillID" />
            <div class="action-buttons">
                <button type="submit" class="btn btn-danger">Xác nhận ẩn</button>
                <a asp-action="Index" class="btn btn-secondary">Hủy</a>
            </div>
        </form>
    </div>
</div>
