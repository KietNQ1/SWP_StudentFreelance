@model StudentFreelance.Models.Skill

@{
    ViewData["Title"] = "Kích hoạt lại kỹ năng";
}

<h2>@ViewData["Title"]</h2>

<h4>Bạn có chắc muốn kích hoạt lại kỹ năng này không?</h4>
<p>
    Kỹ năng sẽ được sử dụng lại trong tuyển dụng, dự án và gán cho sinh viên.
</p>

<div>
    <dl class="row">
        <dt class="col-sm-3">Tên kỹ năng</dt>
        <dd class="col-sm-9">@Model.SkillName</dd>

        <dt class="col-sm-3">Thể loại</dt>
        <dd class="col-sm-9">@Model.Category?.CategoryName</dd>
    </dl>

    <form asp-action="Unhide" method="post">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="SkillID" />
        <button type="submit" class="btn btn-success">Xác nhận kích hoạt lại</button>
        <a asp-action="Index" class="btn btn-secondary">Hủy</a>
    </form>
</div>
